<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ClientOne" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyEditText.Style" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/color_sender</item>
        <item name="android:padding">15dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:drawableStart">@drawable/ic_email_icon</item>
    </style>

    <style name="TextChat" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/color_sender</item>
        <item name="android:textSize">14dp</item>
        <item name="android:drawable">@drawable/background_text</item>
        <item name="android:layout_margin">10dp</item>
    </style>
</resources>